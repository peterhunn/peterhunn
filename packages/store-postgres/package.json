{
  "name": "@x490/store-postgres",
  "version": "0.1.0",
  "description": "Postgres-backed implementations of ContractStore, AuditLog, ApiKeyStore, and WebhookStore",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "private": true,
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "migrate": "tsx src/migrate.ts",
    "test": "echo 'No unit tests — integration tests require a live Postgres instance'"
  },
  "dependencies": {
    "@x490/api": "*",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.7.0"
  }
}
